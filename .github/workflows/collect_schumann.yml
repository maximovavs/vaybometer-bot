name: Collect Schumann Data

on:
  schedule:
    # Запуск каждый час ровно
    - cron: '0 * * * *'
  workflow_dispatch: # Ручной запуск через интерфейс GitHub

jobs:
  schumann:
    runs-on: ubuntu-latest

    steps:
      # Проверяем код репозитория
      - name: 📥 Checkout repo
        uses: actions/checkout@v3

      # Настраиваем Python 3.11
      - name: 🐍 Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      # Устанавливаем зависимости из requirements.txt
      - name: 📦 Install dependencies
        run: |
          pip install -r requirements.txt
          pip install beautifulsoup4

      # Получаем данные резонанса Шумана
      - name: 🚀 Fetch Schumann point
        env:
          TELEGRAM_TOKEN: ${{ secrets.TELEGRAM_TOKEN }}  # Используем существующий секрет
          TELEGRAM_CHAT_ID: ${{ secrets.CHANNEL_ID }}    # Используем CHANNEL_ID как chat_id
        run: |
          python - <<EOF
          import json
          import time
          import requests
          from pathlib import Path
          from bs4 import BeautifulSoup
          from telegram import Bot

          def _fetch_schumann_data(url, attempts=5, backoff=2.0, timeout=45):
              for i in range(attempts):
                  try:
                      response = requests.get(url, timeout=timeout, headers={"User-Agent": "VayboMeter/1.0"})
                      response.raise_for_status()
                      data = response.text
                      print(f"Raw response from {url} (first 200 chars): {data[:200]}...")
                      if data:
                          print(f"Success: Got data from {url}")
                          return data
                      print(f"Retry {i+1}/{attempts} for {url} after {backoff**i}s")
                      time.sleep(backoff**i)
                  except Exception as e:
                      print(f"Retry {i+1}/{attempts} failed for {url}: {e}")
                      time.sleep(backoff**i)
              print(f"All attempts failed for {url}")
              return ""

          try:
              # Пробуем JSON API
              urls = [
                  "https://api.glcoherence.org/v1/earth",
                  "https://api.codetabs.com/v1/proxy?quest=https://api.glcoherence.org/v1/earth",
                  "https://thingproxy.freeboard.io/fetch/https://api.glcoherence.org/v1/earth",
                  "https://api.allorigins.win/raw?url=https://api.glcoherence.org/v1/earth"
              ]
              data = {}
              freq = None
              amp = None

              for url in urls:
                  raw_data = _fetch_schumann_data(url)
                  if raw_data:
                      try:
                          data = json.loads(raw_data) if raw_data.strip().startswith('{') else {}
                          freq = data.get("data", {}).get("sr1", {}).get("frequency_1") or data.get("frequency")
                          amp = data.get("data", {}).get("sr1", {}).get("amplitude_1") or data.get("amplitude")
                          if freq and amp:
                              break
                      except json.JSONDecodeError:
                          print(f"JSON decode failed for {url}, trying HTML parsing")

              # Альтернатива: парсинг HTML с sosrff.tsu.ru
              if not (freq and amp):
                  raw_html = _fetch_schumann_data("https://sosrff.tsu.ru")
                  if raw_html:
                      soup = BeautifulSoup(raw_html, "html.parser")
                      # Пробуем найти данные (примерные селекторы, нужно уточнить)
                      freq_elem = soup.find("div", class_="sr-frequency") or soup.find("span", text=lambda t: "Hz" in t)
                      amp_elem = soup.find("div", class_="sr-amplitude") or soup.find("span", text=lambda t: "nT" in t)
                      freq = freq_elem.text.split()[0] if freq_elem else None
                      amp = amp_elem.text.split()[0] if amp_elem else None
                      print(f"Parsed from sosrff.tsu.ru: freq={freq}, amp={amp}")

              if not (freq and amp):
                  # Fallback на кэш
                  cache = Path("schumann_hourly.json")
                  if cache.exists():
                      arr = json.loads(cache.read_text())
                      if arr:
                          last = arr[-1]
                          freq, amp = last["freq"], last["amp"]
                          print("Using cached data:", last)
                      else:
                          print("Cache exists but is empty")
                  else:
                      print("No cache file found")
                      if "${{ env.TELEGRAM_TOKEN }}" and "${{ env.TELEGRAM_CHAT_ID }}":
                          try:
                              bot = Bot(token="${{ env.TELEGRAM_TOKEN }}")
                              bot.send_message(
                                  chat_id="${{ env.TELEGRAM_CHAT_ID }}",
                                  text="⚠️ Schumann API failed: No data from any URL or cache."
                              )
                          except Exception as e:
                              print(f"Failed to send Telegram notification: {e}")

              if freq and amp:
                  # Сохраняем точку
                  rec = {"ts": int(time.time()), "freq": float(freq), "amp": float(amp)}
                  cache = Path("schumann_hourly.json")
                  arr = json.loads(cache.read_text()) if cache.exists() else []
                  arr.append(rec)
                  cutoff = int(time.time()) - 7*24*3600
                  arr = [r for r in arr if r["ts"] >= cutoff]
                  cache.write_text(json.dumps(arr, ensure_ascii=False))
                  print("Saved Schumann point:", rec)
                  if "${{ env.TELEGRAM_TOKEN }}" and "${{ env.TELEGRAM_CHAT_ID }}":
                      if float(freq) > 8.0 or float(amp) > 100.0:
                          try:
                              bot = Bot(token="${{ env.TELEGRAM_TOKEN }}")
                              bot.send_message(
                                  chat_id="${{ env.TELEGRAM_CHAT_ID }}",
                                  text=f"🚨 High Schumann! Freq: {freq} Hz, Amp: {amp}"
                              )
                          except Exception as e:
                              print(f"Failed to send Telegram notification: {e}")
              else:
                  print("No valid freq/amp data received, even from cache")
          except Exception as e:
              print(f"Error fetching Schumann data: {e}")
              if "${{ env.TELEGRAM_TOKEN }}" and "${{ env.TELEGRAM_CHAT_ID }}":
                  try:
                      bot = Bot(token="${{ env.TELEGRAM_TOKEN }}")
                      bot.send_message(
                          chat_id="${{ env.TELEGRAM_CHAT_ID }}",
                          text=f"⚠️ Schumann script error: {e}"
                      )
                  except Exception as te:
                      print(f"Failed to send Telegram notification: {te}")
          EOF

      # Коммитим обновлённый файл кэша
      - name: 🗂️ Commit cache
        uses: stefanzweifel/git-auto-commit-action@v4
        with:
          commit_message: "schumann: add hourly point"
          file_pattern: "schumann_hourly.json"
