#!/usr/bin/env python3
# -*- coding: utf-8 -*-

"""
gen_lunar_calendar.py
~~~~~~~~~~~~~~~~~~~~~
Генерирует файл lunar_calendar.json для текущего месяца.

Для каждой даты записывает:
  - phase:        иконка луны + название фазы + знак + (N% освещ.)
  - advice:       конкретный призыв к действию
  - next_event:   краткая ссылка на ближайшее событие
  - favorable_days:   список благоприятных дней месяца
  - unfavorable_days: список неблагоприятных дней месяца
"""

import json
import math
import random
from pathlib import Path
import pendulum
import swisseph as swe
import datetime as dt

# Настройки расчёта
SYNODIC_MONTH = 29.530588853

# Иконки фаз
MOON_ICONS = "🌑🌒🌓🌔🌕🌖🌗🌘"

# Знаки зодиака (для Луны)
SIGNS = [
    "Овне", "Тельце", "Близнецах", "Раке", "Льве", "Деве",
    "Весах", "Скорпионе", "Стрельце", "Козероге", "Водолее", "Рыбах",
]

# Заглушечные советы на каждый день (можно расширить)
DAY_ACTIONS = [
    "Начните утро с дыхательной практики 🧘",
    "Выпейте чашечку любимого кофе ☕",
    "Сделайте короткую прогулку на свежем воздухе 🚶",
    "Запланируйте 5-минутный перерыв для растяжки 🤸",
    "Медитируйте перед сном 🌙",
    "Запишите 3 вещи, за которые вы благодарны 🙏",
    "Проведите время без экрана перед сном 📵",
    "Послушайте любимую музыку 🎶",
    "Почитайте книгу на свежем воздухе 📚",
    "Выпейте стакан воды сразу после пробуждения 💧",
]

def compute_phase_info(d: pendulum.Date) -> dict:
    """
    Рассчитывает фазу Луны на дату d:
    • percent — освещённость
    • icon — иконка (bar)
    • name — название фазы
    • sign — знак Зодиака
    """
    # Переводим в UTC для точности
    utc = dt.datetime(d.year, d.month, d.day, tzinfo=dt.timezone.utc)
    jd = swe.julday(utc.year, utc.month, utc.day, utc.hour + utc.minute/60)
    sun_lon = swe.calc_ut(jd, swe.SUN)[0]
    moon_lon = swe.calc_ut(jd, swe.MOON)[0]
    phase_frac = ((moon_lon - sun_lon + 360) % 360) / 360  # 0…1
    illum = round(abs(math.cos(math.pi * phase_frac)) * 100)
    icon = MOON_ICONS[int(phase_frac * 8) % 8]

    # Название фазы
    if illum < 5:
        name = "Новолуние"
    elif illum > 95:
        name = "Полнолуние"
    elif phase_frac < 0.5:
        name = "Растущая Луна"
    else:
        name = "Убывающая Луна"

    sign = SIGNS[int(moon_lon // 30) % 12]

    return {
        "icon":    icon,
        "name":    name,
        "sign":    sign,
        "illum":   illum,
        "text":    f"{icon} {name} в {sign} ({illum}% освещ.)",
    }

def find_next_event(d: pendulum.Date) -> str:
    """
    Заглушка ближайшего события:
    → Через 3 дня частичное солнечное затмение — усиление интуиции
    """
    # здесь можно добавить реальную логику или API
    return "→ Через 3 дня частичное солнечное затмение — усиление интуиции"

def generate_calendar(year: int, month: int) -> dict[str, dict]:
    start = pendulum.date(year, month, 1)
    end = start.end_of('month')
    result: dict[str, dict] = {}
    d = start
    # Пример неблагоприятных дней (каждый 5-й)
    while d <= end:
        info = compute_phase_info(d)
        # конкретный совет: случайный выбор из заглушек
        advice = random.choice(DAY_ACTIONS)

        # Для примера: в качестве благоприятных дней берём все дни недели понедельник–среда
        fav = [i for i in range(1, end.day + 1)
               if pendulum.date(year, month, i).weekday() in (0, 1, 2)]
        # Неблагоприятные — выходные
        unfav = [i for i in range(1, end.day + 1)
                 if pendulum.date(year, month, i).weekday() in (5, 6)]

        result[d.to_date_string()] = {
            "phase":           info["text"],
            "advice":          advice,
            "next_event":      find_next_event(d),
            "favorable_days":   fav,
            "unfavorable_days": unfav,
        }
        d = d.add(days=1)
    return result

def main():
    today = pendulum.today()
    data = generate_calendar(today.year, today.month)
    out = Path(__file__).parent / "lunar_calendar.json"
    out.write_text(json.dumps(data, ensure_ascii=False, indent=2))
    print(f"✅ Файл {out.name} сгенерирован для {today.format('MMMM YYYY')}")

if __name__ == "__main__":
    main()
