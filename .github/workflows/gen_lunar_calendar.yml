name: Generate Lunar Calendar

on:
  # Автоматически 28-го числа каждого месяца в 00:00 UTC
  schedule:
    - cron: '0 0 28 * *'
  # Ручной запуск из интерфейса Actions
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Checkout repository
        uses: actions/checkout@v3
        with:
          # чтобы не было проблем с пушем при автокоммите
          persist-credentials: false

      - name: 🐍 Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: 📦 Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install pendulum pyswisseph

      - name: 🚀 Generate lunar_calendar.json
        run: python gen_lunar_calendar.py

      - name: 📁 Upload lunar_calendar.json
        uses: actions/upload-artifact@v2
        with:
          name: lunar-calendar
          path: lunar_calendar.json
