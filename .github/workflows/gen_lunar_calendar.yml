name: Generate Lunar Calendar

# Запуск вручную через Actions → Run workflow
on:
  workflow_dispatch:

# Разрешаем workflow пушить изменения в репо
permissions:
  contents: write      # чтобы git push мог обновить lunar_calendar.json
  actions: read        # по умолчанию для использования действий

jobs:
  generate:
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Checkout repository
        uses: actions/checkout@v3
        with:
          persist-credentials: true  # чтобы GITHUB_TOKEN использовался для push

      - name: 🐍 Setup Python 3.11
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: 📦 Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install pendulum

      - name: 🌙 Generate lunar_calendar.json
        run: |
          python gen_lunar_calendar.py

      - name: ✏️ Commit lunar_calendar.json back to repo
        uses: stefanzweifel/git-auto-commit-action@v4
        with:
          commit_message: chore: обновить lunar_calendar.json
          file_pattern: lunar_calendar.json
