name: Generate Lunar Calendar

# Запуск вручную или по расписанию — оставил только ручной запуск для отладки
on:
  workflow_dispatch:

# Явно даём runner’у право читать действия (actions) и код (contents)
permissions:
  contents: read
  actions: read

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Checkout repository
        uses: actions/checkout@v3

      - name: 🐍 Setup Python 3.11
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: 📦 Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install pendulum

      - name: 🌙 Generate lunar calendar
        # здесь ваш скрипт, который создаёт файл lunar_calendar.json
        run: python gen_lunar_calendar.py

      - name: 📤 Upload lunar_calendar.json artifact
        uses: actions/upload-artifact@v3
        with:
          name: lunar-calendar
          path: lunar_calendar.json
