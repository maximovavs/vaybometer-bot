name: Generate Lunar Calendar

# Запуск вручную из Actions → Run workflow
on:
  workflow_dispatch:

permissions:
  contents: write   # нужно, чтобы закоммитить обновлённый файл
  actions: read
  metadata: read

jobs:
  generate:
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Checkout repository
        uses: actions/checkout@v3
        with:
          persist-credentials: true  # чтобы git push работал с токеном

      - name: 🐍 Setup Python 3.11
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: 📦 Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install pendulum

      - name: 🌙 Generate lunar_calendar.json
        run: |
          python gen_lunar_calendar.py

      - name: ✏️ Commit lunar_calendar.json back to repo
        uses: stefanzweifel/git-auto-commit-action@v4
        with:
          commit_message: "chore: обновить lunar_calendar.json"
          file_pattern: lunar_calendar.json
