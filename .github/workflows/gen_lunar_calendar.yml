name: Generate Lunar Calendar

# Запуск ручной из Actions → Run workflow
on:
  workflow_dispatch:

# Даем GITHUB_TOKEN право пушить изменения
permissions:
  contents: write

jobs:
  generate:
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Checkout repository
        uses: actions/checkout@v3
        with:
          # чтобы git diff работал корректно
          persist-credentials: true
          fetch-depth: 0

      - name: 🐍 Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: 📦 Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install \ 
            swisseph \
            pendulum \
            requests \
            openai

      - name: 🌙 Generate lunar_calendar.json
        env:
          # если вы хотите, чтобы compute_next_event() обращался к GPT
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
        run: |
          python gen_lunar_calendar.py

      - name: ✏️ Commit and push lunar_calendar.json if changed
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add lunar_calendar.json
          if git diff --cached --quiet; then
            echo "✅ lunar_calendar.json up to date, nothing to commit."
          else
            git commit -m "chore: обновить lunar_calendar.json"
            git push
          fi
