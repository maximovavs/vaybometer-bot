name: Generate Lunar Calendar

# Запуск только вручную (для отладки и единичной генерации)
on:
  workflow_dispatch:

# Безопасные права для скачивания действий и содержимого репозитория
permissions:
  contents: read
  actions: read

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Checkout repository
        uses: actions/checkout@v3

      - name: 🐍 Setup Python 3.11
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: 📦 Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install pendulum

      - name: 🌙 Generate lunar calendar JSON
        run: python gen_lunar_calendar.py

      - name: 📤 Upload lunar_calendar.json
        uses: actions/upload-artifact@v2
        with:
          name: lunar-calendar
          path: lunar_calendar.json
