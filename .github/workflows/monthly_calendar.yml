name: Monthly VayboMeter Calendar

###############################################################################
# 1.  Триггеры
###############################################################################
on:
  schedule:
    - cron: '0 10 * * *'          # каждый день 10:00 UTC
  workflow_dispatch:
    inputs:
      force_run:
        description: "Сгенерировать календарь для текущего месяца"
        required: false
        default: "false"

###############################################################################
permissions:
  contents: write
###############################################################################

jobs:
  monthly:
    runs-on: ubuntu-latest

    steps:
    # -------------------------------------------------------------------------
    # 2.  Checkout
    # -------------------------------------------------------------------------
    - name: 📥 Checkout
      uses: actions/checkout@v4
      with:
        fetch-depth: 0
        persist-credentials: true

    # -------------------------------------------------------------------------
    # 3.  Решаем, нужно ли запускать генератор
    # -------------------------------------------------------------------------
    - name: 🔍 Decide if calendar must be built
      id:   decide
      run: |
        run=no
        # 3-a. cron-ветка: строим только если завтра 1-е
        if [[ "${{ github.event_name }}" == "schedule" ]]; then
          [[ $(date -u -d 'tomorrow' +%d) == "01" ]] && run=yes
        fi
        # 3-b. ручной запуск с force_run=true
        if [[ "${{ github.event_name }}" == "workflow_dispatch" && \
              "${{ github.event.inputs.force_run }}" == "true" ]]; then
          run=yes
        fi
        echo "run_calendar=$run"           >> $GITHUB_OUTPUT
        echo "RUN_CALENDAR=$run"           >> $GITHUB_ENV

    # -------------------------------------------------------------------------
    # 4.  Настраиваем Python
    # -------------------------------------------------------------------------
    - name: 🐍 Setup Python
      if:   env.RUN_CALENDAR == 'yes'
      uses: actions/setup-python@v4
      with:
        python-version: '3.11'

    # -------------------------------------------------------------------------
    # 5.  Ставим зависимости
    # -------------------------------------------------------------------------
    - name: 📦 Install dependencies
      if:   env.RUN_CALENDAR == 'yes'
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt

    # -------------------------------------------------------------------------
    # 6.  Генерируем JSON-календарь
    #     • в последний день месяца → Δ1 день = следующий месяц
    #     • force_run=true          → текущий месяц
    # -------------------------------------------------------------------------
    - name: 🌙 Build lunar_calendar.json
      if:   env.RUN_CALENDAR == 'yes'
      env:
        OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
      run: |
        # Определяем «рабочую» дату, которую передадим скрипту
        if [[ "${{ github.event_name }}" == "schedule" ]]; then
          # для cron-запуска берём завтрашнюю дату (следующий месяц)
          WORK_DATE=$(date -u -d 'tomorrow' '+%Y-%m-01')
        else
          # ручной запуск → сегодняшнее число
          WORK_DATE=$(date -u '+%Y-%m-01')
        fi

        echo "::group::DEBUG date"
        echo "WORK_DATE = $WORK_DATE"
        echo "::endgroup::"

        # Один однострочный python: «подменяем» today()
        python - <<'PY'
import os, pendulum, importlib.util, runpy, sys
# дата, которую мы хотим считать «сегодня»
target = pendulum.parse(os.environ["WORK_DATE"])
# маленький хак: переопределяем pendulum.today() перед import-ом модуля
pendulum.today = lambda tz=None: target
sys.argv = ["gen_lunar_calendar.py"]       # чтобы скрипт считал, что его вызвали напрямую
runpy.run_path("gen_lunar_calendar.py", run_name="__main__")
PY

    # -------------------------------------------------------------------------
    # 7.  Commit & push
    # -------------------------------------------------------------------------
    - name: ✏️ Commit & push
      if:   env.RUN_CALENDAR == 'yes'
      run: |
        git config user.name  "github-actions[bot]"
        git config user.email "github-actions[bot]@users.noreply.github.com"

        git add lunar_calendar.json
        if git diff --cached --quiet; then
          echo "✅ lunar_calendar.json уже актуален"
        else
          git commit -m "chore: update lunar_calendar.json"
          # защищаемся от гонок
          git pull --rebase --autostash || true
          git push
        fi

    # -------------------------------------------------------------------------
    # 8.  Telegram — отправляем месячный отчёт
    # -------------------------------------------------------------------------
    - name: 📨 Send monthly summary
      if:   env.RUN_CALENDAR == 'yes'
      env:
        TELEGRAM_TOKEN: ${{ secrets.TELEGRAM_TOKEN }}
        CHANNEL_ID:     ${{ secrets.CHANNEL_ID }}
      run: python send_monthly_calendar.py
