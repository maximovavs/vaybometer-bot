name: Monthly VayboMeter Calendar

on:
  schedule:
    - cron: '0 10 * * *'     # ежедневно 10:00 UTC
  workflow_dispatch:
    inputs:
      force_run:
        description: "Запустить независимо от даты"
        default: "false"
        required: false

permissions:
  contents: write

env:
  TZ: Asia/Nicosia

concurrency:
  group: monthly-calendar-${{ github.ref }}
  cancel-in-progress: false

jobs:
  monthly_calendar:
    runs-on: ubuntu-latest

    steps:
      - name: 🛎️ Checkout
        uses: actions/checkout@v4

      - name: 🐍 Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: 📦 Install deps
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: 📅 Decide if we should run today
        id: gate
        env:
          FORCE: ${{ github.event.inputs.force_run }}
        run: |
          D=$(date +%d)       # учитывает TZ из env
          if [[ "$FORCE" == "true" || "$D" == "01" ]]; then
            echo "RUN_CALENDAR=yes" >> $GITHUB_ENV
            echo "ℹ️ Calendar run: YES"
          else
            echo "RUN_CALENDAR=no"  >> $GITHUB_ENV
            echo "ℹ️ Calendar run: NO"
          fi

      - name: 🌙 Generate lunar_calendar.json
        if: env.RUN_CALENDAR == 'yes'
        run: python gen_lunar_calendar.py

      - name: 💾 Commit updates (if any)
        if: env.RUN_CALENDAR == 'yes'
        run: |
          git config user.name  "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add lunar_calendar.json
          if git diff --cached --quiet; then
            echo "✅ lunar_calendar.json актуален"
          else
            git commit -m "chore: update lunar_calendar.json"
            git pull --rebase --autostash || true
            git push
          fi

      - name: 📨 Send monthly summary
        if: env.RUN_CALENDAR == 'yes'
        env:
          TELEGRAM_TOKEN: ${{ secrets.TELEGRAM_TOKEN }}
          CHANNEL_ID:     ${{ secrets.CHANNEL_ID }}
        run: python send_monthly_calendar.py
