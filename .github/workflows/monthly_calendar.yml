name: Monthly VayboMeter Calendar

on:
  schedule:
    - cron: '0 10 * * *'           # каждый день 10:00 UTC
  workflow_dispatch:
    inputs:
      force_run:
        description: "Запустить независимо от даты"
        required:    false
        default:     "false"

permissions:
  contents: write

jobs:
  monthly:
    runs-on: ubuntu-latest

    steps:
    # 1) Checkout
    - uses: actions/checkout@v4
      with: {fetch-depth: 0}

    # 2) Определяем, надо ли выполнять расчёт
    - name: 🔍 Decide whether to run calendar
      id:   decide
      run: |
        # true, если завтра 1-е число
        [[ $(date -d 'tomorrow' +%d) == "01" ]] && run=yes || run=no

        # ручной запуск с force_run=yes тоже включает
        if [[ "${{ github.event_name }}" == "workflow_dispatch" && \
              "${{ github.event.inputs.force_run }}" == "true" ]]; then
          run=yes
        fi

        echo "run_calendar=$run" >> $GITHUB_OUTPUT
        echo "RUN_CALENDAR=$run"  >> $GITHUB_ENV   # удобно для if:

    # 3) Python
    - uses: actions/setup-python@v4
      with: {python-version: '3.11'}

    # 4) Deps
    - name: 📦 Install dependencies
      if: env.RUN_CALENDAR == 'yes'
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt

    # 5) Генерация
    - name: 🌙 Generate lunar_calendar.json
      if: env.RUN_CALENDAR == 'yes'
      env:
        OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
      run: python gen_lunar_calendar.py

    # 6) Commit & push
    - name: ✏️ Commit & push
      if: env.RUN_CALENDAR == 'yes'
      run: |
        git config user.name  "github-actions[bot]"
        git config user.email "github-actions[bot]@users.noreply.github.com"
        git add lunar_calendar.json
        git diff --cached --quiet || {
          git commit -m "chore: update lunar_calendar.json"
          git pull --rebase --autostash || true
          git push
        }

    # 7) Telegram
    - name: 📨 Send monthly summary
      if: env.RUN_CALENDAR == 'yes'
      env:
        TELEGRAM_TOKEN: ${{ secrets.TELEGRAM_TOKEN }}
        CHANNEL_ID:     ${{ secrets.CHANNEL_ID }}
      run: python send_monthly_calendar.py
