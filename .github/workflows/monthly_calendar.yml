name: Monthly VayboMeter Calendar

###############################################################################
# 1. События-триггеры
###############################################################################
on:
  schedule:
    #   мин   час  день  мес  день-нед
    - cron:  '0 10 * * *'          # каждый день, 10 UTC
  workflow_dispatch:               # ← обязательная пустая строка!
    inputs:
      force_run:
        description: "Сгенерировать календарь на текущий месяц (игнорировать дату)"
        required: false
        default: "false"

###############################################################################
permissions:
  contents: write
###############################################################################

jobs:
  monthly:
    runs-on: ubuntu-latest

    steps:
    # ------------------------------------------------------------------ 1
    - name: 📥 Checkout
      uses: actions/checkout@v4
      with:
        fetch-depth: 0
        persist-credentials: true

    # ------------------------------------------------------------------ 2
    - name: 🔍 Decide if calendar must be built
      id: decide
      run: |
        run=no
        # (а) автозапуск: строим, если завтра 1-е
        if [[ "${{ github.event_name }}" == "schedule" ]]; then
          [[ $(date -u -d 'tomorrow' +%d) == 01 ]] && run=yes
        fi
        # (б) ручной run + force_run=true
        if [[ "${{ github.event_name }}" == "workflow_dispatch" && \
              "${{ github.event.inputs.force_run }}" == "true" ]]; then
          run=yes
        fi
        echo "RUN_CALENDAR=$run" >>"$GITHUB_ENV"

    # ------------------------------------------------------------------ 3
    - name: 🐍 Setup Python
      if: env.RUN_CALENDAR == 'yes'
      uses: actions/setup-python@v4
      with:
        python-version: '3.11'

    # ------------------------------------------------------------------ 4
    - name: 📦 Install dependencies
      if: env.RUN_CALENDAR == 'yes'
      run: |
        python -m pip install -U pip
        pip install -r requirements.txt

    # ------------------------------------------------------------------ 5
    - name: 🌙 Generate lunar_calendar.json
      if: env.RUN_CALENDAR == 'yes'
      env:
        OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
      run: |
        # → автозапуск: завтра 1-е → берём следующий месяц
        # → ручной    : всегда текущий месяц
        if [[ "${{ github.event_name }}" == "schedule" ]]; then
          WORK_DATE=$(date -u -d 'tomorrow' +%Y-%m-01)
        else
          WORK_DATE=$(date -u +%Y-%m-01)
        fi

        python - <<'PY'
import os, pendulum, runpy, sys
pendulum.today = lambda tz=None: pendulum.parse(os.environ["WORK_DATE"])
sys.argv = ["gen_lunar_calendar.py"]
runpy.run_path("gen_lunar_calendar.py", run_name="__main__")
PY

    # ------------------------------------------------------------------ 6
    - name: ✏️ Commit & push
      if: env.RUN_CALENDAR == 'yes'
      run: |
        git config user.name  "github-actions[bot]"
        git config user.email "github-actions[bot]@users.noreply.github.com"
        git add lunar_calendar.json
        if git diff --cached --quiet; then
          echo "✅ Файл уже актуален"
        else
          git commit -m "chore: update lunar_calendar.json"
          git pull --rebase --autostash || true
          git push
        fi

    # ------------------------------------------------------------------ 7
    - name: 📨 Send monthly summary
      if: env.RUN_CALENDAR == 'yes'
      env:
        TELEGRAM_TOKEN: ${{ secrets.TELEGRAM_TOKEN }}
        CHANNEL_ID:     ${{ secrets.CHANNEL_ID }}
      run: python send_monthly_calendar.py
