# ── базовые утилиты ─────────────────────────────────────────────
requests>=2.32
python-dateutil>=2.9
pendulum>=3.0.0      # мы уже адаптировали код под v3
pandas>=2.2
pyswisseph>=2.10         # пакет даёт модуль 'swisseph'
astral>=2.2
jinja2>=3.1,<4           # ← добавь эту строку (рендер шаблонов)

# Если нужна астропакета — оставь следующую строку, иначе удали:
# astropy>=6,<7

# ── бот и LLM ───────────────────────────────────────────────────
python-telegram-bot>=20,<21
openai>=1.6,<2
google-generativeai>=0.7.2
groq>=0.9.0
tweepy>=1.0,<2
